<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.yang.nebula.agent.business.file.share.mapper.FileSharingAssociationMapper">

    <resultMap id="BaseResultMap" type="cn.yang.nebula.agent.business.file.share.dal.FileSharingAssociationDo">
        <id column="id" property="id" />
        <result column="share_id" property="shareId" />
        <result column="file_id" property="fileId" />
        <result column="space_id" property="spaceId" />
    </resultMap>

    <sql id="Base_Column_List">
        id, share_id, file_id, space_id
    </sql>

    <insert id="insertBatch">
        INSERT INTO file_sharing_association (id, share_id, file_id, space_id)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.shareId}, #{item.fileId}, #{item.spaceId})
        </foreach>
    </insert>

    <delete id="deleteByShareId">
        DELETE FROM file_sharing_association WHERE share_id = #{shareId}
    </delete>

    <select id="selectByShareId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM file_sharing_association
        WHERE share_id = #{shareId}
    </select>

</mapper>
